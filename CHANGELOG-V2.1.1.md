# UP Entregas v2.1.1

- Corrige falha de compilação em `MainActivity.java` causada por referência residual a `offerTone` no `onDestroy()`.
- Mantém todas as funções da v2.1.0: UP Central, rotas múltiplas, complemento de rota, telemetria, códigos e mapa.
- `versionCode` atualizado para 211 e `versionName` para `2.1.1-central-rotas-fix`.
